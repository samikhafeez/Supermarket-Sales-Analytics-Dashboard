"""Business logic services for the supermarket analytics dashboard.

Import the specific module you need (e.g. ``from services import analytics``)
rather than relying on re-exports here. Avoiding eager re-imports keeps the
dependency surface of each submodule explicit - for example, importing the
analytics service does not pull in plotly.
"""
